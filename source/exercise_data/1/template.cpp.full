// id = UNIQUE_ID

#include <cstdio>

using namespace std;

struct segment_tree {
	static int get_segment_tree_size(int input_size) {
USER_CODE
	}
};

segment_tree tree;

int main() {	
	int n; scanf("%d", &n);
	
	for (int i = 0 ; i < n ; i++) {
		int sz; scanf("%d", &sz);
		printf("%d ", segment_tree::get_segment_tree_size(sz));
	}
}